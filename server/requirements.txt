fastapi==0.115.8
uvicorn==0.34.0
pydantic[email]==2.13.4  # mcp==2.0.0 requires pydantic>=2.12.0
mem0ai>=0.1.48
python-dotenv==1.2.2
psycopg>=3.2.8
psycopg-pool>=3.2.6,<4.0.0

# Bundled LLM/embedder providers.
anthropic>=0.39,<1.0
ollama>=0.3,<1.0

# MCP Streamable HTTP endpoint (central-mcp-spike)
# sse-starlette pinned: mcp's own bound (>=3.0.0) resolves to a version
# requiring starlette>=0.49.1, which conflicts with fastapi's starlette<0.46.0 pin above.
mcp==2.0.0
sse-starlette==3.0.0

# Auth & database (Phase 1)
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
passlib[bcrypt]>=1.7,<2.0
bcrypt==4.0.1
python-jose[cryptography]>=3.3,<4.0
cryptography>46.0.4

# Anonymous telemetry — on by default, opt out with MEM0_TELEMETRY=false
posthog>=3.0,<8.0

# Per-IP rate limiting on auth endpoints
slowapi>=0.1.9,<1.0
